<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="latest.pankaj.utility">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".otp_pin_view.OtpViewExampleActivity" />
        <activity android:name=".permissions_utils.PermissionExampleActivity" />
        <activity android:name=".drawing_view.DrawingViewActivity" />
        <activity android:name=".otp_pin_view_2.PinViewActivity2" />
        <activity android:name=".credit_card_view.CreaditCardActivity" />
        <activity android:name=".gauge_view.GaugeViewExampleActivity" />
        <activity android:name=".permission_utils_2.utils.PermissionExample2Activity" />
        <activity android:name=".rings_view.RingsViewExampleActivity" />
        <activity android:name=".toast_custom.lib_files.ToastyExampleActivity" />
        <activity android:name=".show_case_view.ShowCaseViewExampleActivity" />
        <activity android:name=".time_line_view.example.TimeLineMainActivity" />
        <activity android:name=".time_line_view.example.TimeLineActivity" />
        <activity android:name=".compaas_easy.CompassActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".google_map.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".meter_view.MeterViewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".progress_view.demo.ProgressViewDemoActivity2" />
        <activity android:name=".progress_view.demo.ProgressViewDemoActivity1" />


    </application>

</manifest>